---
# If you want to generate secret with python use:
# from os import urandom; from binascii import hexlify; print(hexlify(urandom(24)).decode())
SECRET_KEY: "Something"
POSTGRES_URL: "127.0.0.1:5432"
POSTGRES_USER: "postgres"
POSTGRES_PW: "root"
POSTGRES_DB: "re_api"
